export const DEFAULT_PROJECT_ID = "default";
export const SUPPORTED_TRANSITIONS = ["fade", "crossfade"] as const;
export const TOOL_NAMES = [
  "add_clip",
  "remove_clip",
  "move_clip",
  "trim_clip",
  "split_clip",
  "add_transition",
  "add_overlay",
  "set_audio_level",
  "export_project"
] as const;
